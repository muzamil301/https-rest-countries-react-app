// import { getAllCountriesData } from "./CountriesAdapter";
export const c = {};

describe("Countries listing data", () => {
  describe("getAllCountriesData", () => {
    it("should return an array with length > 0", () => {
      const countries = 2;
      expect(countries).toBe(2);
    });
  });
});
